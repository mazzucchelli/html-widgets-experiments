import { RC } from "../../../lib/helpers/ReactiveComponent";

export default (ctx: RC<{}, {}>) => {
  const { $el } = ctx;

//   $el.addEventListener("click", () => {
//     console.log("clicked", $el, newComp);
//     document.body.querySelector("[data-r-root]").appendChild(newComp);
//   });
};
